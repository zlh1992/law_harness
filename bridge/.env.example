# Harness 连接本地代理时使用的本地口令；不是 OpenAI Platform API Key。
DSH_PROXY_TOKEN=请替换为随机高强度口令

PROXY_HOST=127.0.0.1
PROXY_PORT=4010
CODEX_MODEL=gpt-5.6-sol

# 订阅通道默认串行，避免同时占用过多 Codex 配额。
MAX_CONCURRENCY=1
CODEX_TIMEOUT_MS=300000
MAX_BODY_BYTES=33554432
